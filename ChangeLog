2015-12-08    <Skrylar@SKRYLAR-PC>

	* red.el (red-mode): Disable electric indentation by default.
	(red-get-indentation-for-line): Don't dedent a closer if there is
	other text on the same line; will want to adjust this to account
	for closers at the beginning of a line, though.

2015-11-02    <Skrylar@SKRYLAR-PC>

	* red.el (red-percent-regex): Regex to find percentages.
	(red-tuple-regex): Regex to find tuples.
	(red-pair-regex): Regex to find pairs.
	(red-font-lock-keywords): Add additional types to font lock.

2015-11-01    <Skrylar@SKRYLAR-PC>

	* red.el (red-font-lock-keywords): Add font locks for integers,
	hexadecimal and floating point values.
	(red-integer-regex): Definition of integers.
	(red-hexadecimal-regex): Definition of hexadecimal integers.
	(red-float-regex): Definition of floating point numbers.

2015-10-11    <Skrylar@SKRYLAR-PC>

	* red.el (red-mode): Change from fundamental-mode to prog-mode.
	(red-syntax-table): Change from "synTable" to "syn-table."
	Add GPLv3 licensing (same as all emacs plugins.)
